[package]
name = "billion_rows_challenge"
version = "0.1.0"
edition = "2024"

[dependencies]

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"

[profile.samply]
inherits = "release"
debug = true
